// Update this if a custom domain is attached later (Vercel project settings),
// then regenerate public/sitemap.xml (npm run generate:sitemap).
export const SITE_URL = "https://git-github-doc.vercel.app"
export const SITE_NAME = "Git & GitHub Learn"
export const DEFAULT_DESCRIPTION =
  "Learn Git and GitHub for free with short, visual lessons — commits, branching, merging, rebasing, remotes, undoing mistakes, pull requests, and GitHub Actions."
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png`
